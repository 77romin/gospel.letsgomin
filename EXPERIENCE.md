# Gospel Choir Practice 개발 경험 정리

## 프로젝트 배경

성가대원이 전체 합창 음원만 반복해서 듣는 대신 **자신의 파트를 선택하고 필요한 구간으로 바로 이동할 수 있는 연습실**을 만들었다. 지휘자는 음악을 들으며 공통 연습 구간을 표시하고, 성가대원은 저장된 구간을 개인 연습에 사용할 수 있게 했다.

현재 공개 버전은 개인 연습에 집중한다. 함께 듣는 기능도 구현하고 실제 기기에서 시험했지만 모바일 청취자의 소리가 지휘자보다 늦게 들리는 문제를 충분히 해결하지 못해 UI에서 숨겼다. 이 결정은 기능의 구현 여부와 실제 사용 품질을 구분한 결과다.

## 구현 범위

- 다섯 파트의 MP4 음원을 선택하고 재생·일시정지·위치 이동할 수 있는 반응형 플레이어를 만들었다.
- 영상이 있는 음원은 같은 `<video>` 요소로 소리와 화면을 재생한다. `Show Video`를 눌러 영상을 열면 재생 막대가 얇아지고, 닫아도 재생 위치는 유지된다.
- 지휘자가 일시정지 위치에서 시작과 종료를 찍어 구간을 만들고 이름·색상을 지정하도록 했다. 저장된 구간의 수정·강조·완료·삭제와 구간 클릭 이동도 구현했다.
- 로컬 Node.js·WebSocket 프로토타입을 Vercel 정적 배포와 Supabase Auth·PostgreSQL·Realtime 구성으로 옮겼다. 지휘자 권한 확인과 구간 쓰기 제한은 데이터베이스 함수와 접근 정책에 두었다.
- Route 53의 `gospel` CNAME을 Vercel에 연결해 기존 WordPress용 Lightsail 인스턴스와 분리된 서브도메인으로 배포했다.

## 현재 결과

- `gospel.letsgomin.com`에서 다섯 파트의 개인 연습과 구간 이동을 사용할 수 있다.
- 지휘자는 같은 화면에서 구간을 관리하고, 성가대원은 저장된 구간을 자신의 재생 위치 이동에 사용한다.
- 공동 재생은 끊김을 줄였지만 기기 간 들리는 시점 차이가 남아 공개 UI에서 제외했다.

## 문제와 해결 과정

### 1. 시간을 직접 입력해야 하는 구간 설정

**문제:** 지휘자가 재생 위치를 확인해 시작·종료 시간을 텍스트로 적는 방식은 연습 중 쓰기 번거롭고 오입력 가능성이 있었다.

**시도와 결과:** 처음에는 재생 막대에 마우스로 핀을 찍는 방안을 검토했다. 그러나 작은 화면에서 정확한 지점을 찍기 어렵다는 피드백을 반영해, 음악을 멈춘 뒤 `구간 시작`과 `구간 종료`를 누르는 방식으로 바꿨다. 이름과 색상을 지정하고 기존 구간을 고칠 수 있도록 했다. 현재 지휘자는 개인 연습 화면에서도 구간을 관리할 수 있다.

### 2. 좁은 창에서 플레이어 배치가 깨짐

**문제:** 영상 버튼, 재생 버튼, 볼륨과 상태 버튼을 한 줄에 배치했을 때 창이 좁아지면 간격이 크게 벌어지거나 요소가 엉뚱한 위치로 내려갔다.

**시도와 결과:** 데스크톱에서는 재생 컨트롤을 가운데, 영상 버튼을 재생 막대의 왼쪽 아래, 볼륨을 오른쪽에 두었다. 작은 화면에서는 CSS 그리드가 한 열로 바뀌도록 해 각 컨트롤이 순서대로 쌓이게 했다. 영상 표시 시에도 같은 플레이어를 유지하고 재생 막대 높이만 조정했다.

### 3. 무료 배포와 권한 분리

**문제:** 기존 WordPress Lightsail 인스턴스는 RAM 512MB였고, 별도 서버 비용을 추가하고 싶지 않았다. 로컬 Node 서버의 메모리 상태와 WebSocket 연결을 그대로 정적 호스팅에 옮길 수도 없었다.

**시도와 결과:** 음원과 화면은 Vercel에서 정적으로 제공하고, 지휘자 로그인·구간 데이터·변경 알림은 Supabase로 분리했다. 구간 조회는 공개하되 쓰기는 지휘자 계정으로 제한했다. Route 53에는 서브도메인 CNAME만 추가해 WordPress 도메인 설정을 유지했다. 다만 무료 플랜의 용량·전송량과 프로젝트 활동 상태는 운영 중 계속 확인해야 한다.

### 4. 모바일 공동 재생의 끊김과 시간 차이

**문제:** 초기 공동 재생에서는 휴대폰 청취자가 자주 끊기고 지휘자보다 늦게 들렸다. 처음에는 약 2초, 개선 후에도 귀로 약 0.5초 차이가 들렸다. 한 차례 진단에서 각 기기의 서버 기준 위치 대비 컴퓨터 음원은 약 `-0.12초`, 휴대폰 음원은 약 `-0.71초`로 표시됐다. 이는 당시 기기에서 관찰한 값이며 모든 환경의 고정 지연값은 아니다.

**시도:**

1. 재생 중 주기적으로 `currentTime`을 강제로 옮기던 동작을 없애고, 참여·재생·일시정지·위치 이동 명령이 왔을 때만 서버 상태를 반영했다. 이 뒤 끊김은 사라졌지만 시간 차이는 남았다.
2. 서버 시각을 여러 번 측정해 왕복 시간이 가장 짧은 값을 사용하고, 실제 `playing` 및 `seeked` 이벤트 뒤에 한 차례 위치를 보정했다.
3. 짧은 배속 재생으로 뒤처진 시간을 따라잡는 방법도 시도했으나 사용자가 듣는 차이를 해결하지 못했다.
4. 지휘자 재생과 재생 중 위치 이동을 3초 뒤로 예약하고, 그 전에 음원 로딩과 탐색을 마치는 방식도 구현했다. 늦게 참여한 청취자에게도 로컬 준비 시간을 주었다.

**현재 결과:** 준비 시간을 늘리는 방식이 모든 휴대폰에서 들리는 소리를 충분히 맞추는지는 검증되지 않았다. 웹 플레이어의 표시 위치, 브라우저가 `playing`을 알리는 시각, 실제 스피커 출력 시각을 따로 측정할 필요가 있다. 사용자 경험을 우선해 공동 재생 탭과 참여 버튼은 숨기고 개인 연습 기능을 공개했다. 동기화 코드는 향후 실험을 위해 남겨 두었다.

## 이번 프로젝트에서 배운 점

- 화면에서 보이는 재생 위치가 비슷하다는 사실만으로 여러 기기의 소리가 동시에 나온다고 판단할 수 없다. 실제 청취 비교와 이벤트·시계·미디어 위치 진단을 함께 봐야 한다.
- 동기화 개선을 평가할 때 **끊김**과 **시간 차이**를 별도 지표로 다뤄야 한다. 이벤트 기반 전환은 끊김을 해결했지만 시간 차이는 해결하지 못했다.
- 제품에 노출할 기능은 구현 완료 여부보다 실제 연습 상황에서 쓸 수 있는 품질을 기준으로 결정해야 한다. 미해결 기능을 숨긴 뒤 지휘자 구간 설정과 개인 연습 흐름을 완성했다.

## 다음 검증 과제

- 동일 음원의 시작 신호를 여러 기기에서 녹음해 실제 소리 출력 시각을 비교한다.
- 재생 요청, `playing` 이벤트, `currentTime`, 스피커 출력 지연을 분리해 기록한다.
- 공동 재생을 다시 공개하기 전에 기기·브라우저·네트워크 조건별 허용 오차와 재현 테스트를 정한다.

구현 코드와 실행 방법은 [README](README.md), 배포 구성은 [DEPLOY](DEPLOY.md)를 참고한다.
