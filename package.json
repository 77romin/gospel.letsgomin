{
  "name": "gospel-choir-practice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node scripts/check-cloud-env.js && vite build && node scripts/copy-static.js",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
